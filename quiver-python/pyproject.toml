[project]
name = "quiver-python"
version = "0.1.0"
description = "Python client for Quiver gRPC server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "grpcio-tools>=1.78.0",
    "pyarrow>=23.0.1",
]

[dependency-groups]
dev = [
    "ruff>=0.4.0",
    "pytest>=7.0.0",
]
